{% if current_time is defined %}
#  Generated at {{ current_time }}
{% endif %}
# Service-specific configuration file for tomcat. This will be sourced by
# the SysV init script after the global configuration file
# /etc/tomcat/tomcat.conf, thus allowing values to be overridden in
# a per-service manner.
#
# TEST change the init script itself. To change values for all services make
# your changes in /etc/tomcat/tomcat.conf
#
# To change values for a specific service make your edits here.
# To create a new service create a link from /etc/init.d/<your new service> to
# /etc/init.d/tomcat (do not copy the init script) and make a copy of the
# /etc/sysconfig/tomcat file to /etc/sysconfig/<your new service> and change
# the property values so the two services won't conflict. Register the new
# service in the system as usual (see chkconfig and similars).
#

# Where your java installation lives
#JAVA_HOME="/usr/lib/jvm/java"

# Where your tomcat installation lives
CATALINA_BASE="/opt/TEST/tomcat/TEST"
CATALINA_HOME="/usr/share/tomcat"
CATALINA_TMPDIR="/opt/TEST/tomcat/TEST/temp"

#Where your EL UI is hosted
#EL_UI_HOSTNAME="{{ el_ui_fqdn }}"

CATALINA_OPTS="-Xmx2048m -Xms512m -XX:MaxPermSize=512m \
-DAUDIT_MSG_PATH=/opt/TEST/TEST/TEST \
-DTEST_HOME=/opt/TEST/TEST/base \
-DrcFile=/opt/TEST/TEST/.mds.rc \
-DdbFile=/opt/TEST/TEST/etc/db/db.properties \
-DTEST.documentation.root=/opt/TEST/TEST/documentation \
-Dcom.TEST.ccp.ria.audit.solution.properties={{ audittrail_config_file }} \
-Dcom.TEST.um.shiro.cache.configuration.file=file:///opt/TEST/TEST/TEST-ui/shiro-ehcache-configuration.xml \
-Dkeycloak.config.file=/opt/TEST/tomcat/TEST/conf/keycloak.json \
-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=59088"

# You can pass some parameters to java here if you wish to
#JAVA_OPTS="-Xminf0.1 -Xmaxf0.3"

# Use JAVA_OPTS to set java.library.path for libtcnative.so
#JAVA_OPTS="-Djava.library.path=/usr/lib64"

# What user should run tomcat
TOMCAT_USER="TEST"

# You can change your tomcat locale here
#LANG="en_US"

# Run tomcat under the Java Security Manager
#SECURITY_MANAGER="false"

# Time to wait in seconds, before killing process
#SHUTDOWN_WAIT="30"

# Whether to annoy the user with "attempting to shut down" messages or not
#SHUTDOWN_VERBOSE="false"

# Set the TOMCAT_PID location
CATALINA_PID="/opt/TEST/tomcat/TEST/tomcat.pid"

# Connector port is 8080 for this tomcat instance
CONNECTOR_PORT="55555"

# If you wish to further customize your tomcat environment,
# put your own definitions here
# (i.e. LD_LIBRARY_PATH for some jdbc drivers)
